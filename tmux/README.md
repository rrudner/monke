# Tmux component

Optional tmux configuration for persistent remote work: mouse support, a large history, and safe
SSH auto-attach to the `main` session. Tmux must already be installed. Scriptorium sources its
own fragment from a marker-delimited block while preserving user settings. Use `NO_AUTO_TMUX=1`
to skip auto-attach for one shell.
