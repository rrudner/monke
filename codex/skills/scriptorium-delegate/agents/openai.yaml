interface:
  display_name: "Delegate"
  short_description: "Route bounded work to efficient Codex models"
  default_prompt: "Use $scriptorium-delegate to offload this bounded task to the cheapest suitable model."
policy:
  allow_implicit_invocation: true
